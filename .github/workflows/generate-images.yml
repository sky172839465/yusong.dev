name: generate-images

on:
  workflow_dispatch:
  pull_request:
    types: [opened, reopened, synchronize]

concurrency:
  group: "generate-images"
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write
  id-token: write
  actions: write

jobs:
  check-changes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for changes in src/
        id: check_changes
        run: |
          if git diff --name-only origin/main...HEAD | grep -q '^src/.*/index\.(md|meta\.js)$'; then
            echo "Changes detected in src/, continuing workflow."
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "No changes in src/, stopping workflow."
            exit 0
          fi

  generate-images:
    needs: check-changes
    if: needs.check-changes.outputs.changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.REPO_TOKEN }}

      - name: Render images for every Mermaid diagram in all Markdown files (*.md) in the repo
        uses: nielsvaneck/render-md-mermaid@v3

      - name: Generate RWD / OG / Twitter image
        run: |
          npm install
          npm run gen:data

      - name: Check for changes
        id: git_diff
        run: |
          git status --porcelain | grep -E '\.(jpg|jpeg|png|gif|svg|webp)$' && echo "changed=1" >> $GITHUB_OUTPUT || echo "changed=0"

      - name: Commit if images generated
        if: ${{ steps.git_diff.outputs.changed == 1 }}
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.number }}
        run: |
          git config --global user.name "${GITHUB_ACTOR}"
          git config --global user.email "${GITHUB_ACTOR_ID}+${GITHUB_ACTOR}@users.noreply.github.com"
          git add .
          git commit -m "chore: automatically generate images"
          git push
          gh pr comment ${{ env.PR_NUMBER }} --body "🤖 Generate images, commit: $(git rev-parse --short HEAD)."
